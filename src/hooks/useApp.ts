import { useApp as useAppContext } from '../context/AppContext';

/**
 * Hook to access the centralized CampusEats Application Context
 */
export const useApp = useAppContext;

export default useApp;
